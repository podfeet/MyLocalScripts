#!/bin/zsh

# reset M3 Max LocalHostName without incrementing number
sudo scutil --set LocalHostName Al-M3-Max

# reset M3 Max ComputerName without incrementing number
sudo scutil --set ComputerName Al-M3-Max
